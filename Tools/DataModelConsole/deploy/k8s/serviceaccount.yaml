apiVersion: v1
kind: ServiceAccount
metadata:
  name: console-api
  namespace: console
  # No IRSA role-arn annotation: S3 access is granted via EKS Pod Identity
  # (aws_eks_pod_identity_association in deploy/terraform/main.tf binds this
  # SA to the console-api role). Pod Identity does not use the SA annotation.
